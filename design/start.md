Role & Objective
Act as a Senior UI/UX Designer and Frontend Developer.

Your goal is to convert the attached reference image into a pixel-perfect Hero section using code.

# Tech Stack & Constraints
- Use plain HTML5, CSS3, and modern JavaScript (ES6+).
- No frameworks (React/Vue/Bootstrap).
- Use Semantic HTML (e.g., <header>, <nav>, <section>, <h1>).
- Use CSS Variables (:root) for all colors, font sizes, and spacing.
- Use CSS Flexbox and CSS Grid for layout.
- Ensure the design is fully responsive.

# Design & Assets Instructions
1. Visual Analysis:
   - Extract Hex colors from the reference.
   - Identify typography (use Google Fonts alternative if needed).
   - Match spacing, border-radius, and shadows precisely.

2. Icons:
   - Use Iconify Solar Linear icons if required.

Only implement:
- Header
- Hero section

Stop after Hero.
